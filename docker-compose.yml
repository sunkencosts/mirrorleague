services:
  db:
    image: postgres:17
    environment: 
      POSTGRES_USER: mirrorme
      POSTGRES_PASSWORD: mirrorme
      POSTGRES_DB: mirrorme
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
  
volumes:
  pgdata: